# Dependencias con versión mínima fijada (reproducibilidad / cadena de suministro).
# Para builds 100% reproducibles, generar un lock con: pip freeze > requirements.lock
Flask>=3.0,<4.0
flask-limiter>=3.5          # rate limiting por IP
requests>=2.31,<3.0
certifi>=2024.2.2
cryptography>=42.0
truststore>=0.8;python_version>="3.10"
# Exportadores (opcionales pero recomendados)
python-docx>=1.1
reportlab>=4.1
# Producción (Windows): waitress-serve --threads=16 app:app
waitress>=3.0
# Análisis de código: descompresión de archivos subidos
py7zr>=0.21          # soporte .7z (puro Python)
rarfile>=4.0         # soporte .rar (requiere binario unrar/bsdtar en el sistema)
# Análisis avanzado opcional (motores SAST OSS): ver requirements-advanced.txt
#   pip install -r requirements-advanced.txt
